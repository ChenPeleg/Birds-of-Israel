import React from "react";
import styles from './birdPhoto.module.scss';

const BirdSound = (props) => {

    const assets = require.context('../assets/sounds', true);
    // const loadSound = imageName => (assets(`./${imageName}`).default);

    return (<div />)
}
export default BirdSound;